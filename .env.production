# Production build config — committed on purpose.
# Vite inlines VITE_* vars into the client bundle at build time, so these are
# public by design and contain no secrets. Loaded automatically by `vite build`.
VITE_API_URL=https://fly-desk-backend.onrender.com/api
